express-forever-test
====================

A simple example to use forever with a nodejs express server.

## Setup

``` bash
  $ [sudo] npm install forever -g
```

## Usage

``` bash
  $ forever server.js
```
